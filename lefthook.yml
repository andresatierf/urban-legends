pre-commit:
  parallel: true
  commands:
    oxlint:
      glob: "*.{js,ts,jsx,tsx}"
      exclude: "convex/_generated/**"
      run: bun --bun oxlint {staged_files}
    oxfmt:
      glob: "*.{js,ts,jsx,tsx}"
      exclude: "convex/_generated/**"
      run: bun --bun oxfmt --write {staged_files}
      stage_fixed: true
    typecheck:
      glob: "*.{js,ts,jsx,tsx}"
      exclude:
        - "convex/_generated/**"
        - ".sandcastle/**"
      run: bun --bun tsc-files --noEmit {staged_files} src/routeTree.gen.ts
post-checkout:
  commands:
    install:
      run: bun install
